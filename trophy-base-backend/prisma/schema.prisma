generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Trofeo {
  id         Int      @id @default(autoincrement())
  nombre     String
  juego      String
  dificultad String
  requisito  String
  estado     Boolean @default(false)
  imagen     String
}